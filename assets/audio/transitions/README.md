Place licensed transition effects here, for example:

- `whoosh.mp3`
- `deep-boom.wav`

The backend FFmpeg finishing step automatically discovers `.mp3` and `.wav` files in this directory and layers them at scene boundaries.
