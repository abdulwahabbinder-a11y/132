"""AI media generation clients (NVIDIA NIM-hosted models).

All modules expose async functions that return a local file path (or remote
URL) to the generated artifact. Heavy/long-running calls use polling helpers.
"""
