# Pipeline Assets

Drop the following royalty-free assets here for full audio post-production:

- `whoosh.mp3` — short transition swoosh / deep boom played between cuts.
- `background.mp3` — instrumental score auto-ducked under narration.

These files are excluded from version control via `.gitignore`. Replace them
with licensed assets in production environments.
