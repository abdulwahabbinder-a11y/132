import { PricingSection } from "@/components/PricingSection";

export const metadata = {
  title: "Pricing — DocuForge AI",
};

export default function PricingPage() {
  return (
    <div className="bg-hero-grid">
      <PricingSection />
    </div>
  );
}
