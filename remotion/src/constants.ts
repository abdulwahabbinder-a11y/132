// Cinematic 21:9 master format.
export const FPS = 30;
export const WIDTH = 2560;
export const HEIGHT = 1097; // 2560 / (21/9) ≈ 1097

// Fallback per-scene duration (seconds) when no narration audio is present.
export const DEFAULT_SCENE_SECONDS = 5;

// Cross-fade between scenes (frames).
export const TRANSITION_FRAMES = 18;
